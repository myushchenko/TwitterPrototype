/// <reference path="index.d.ts" />

module App {
    'use strict';

    var moduleApp = angular.module('App', ['ngResource']);

    moduleApp.run(['$rootScope', ($rootScope: ng.IRootScopeService) => {
        
    }]);
}